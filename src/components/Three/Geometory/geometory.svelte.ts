export const GeometorySettings = $state({
  isEdge: true,
  isRotation: true,
  isNormalMaterial: false,
  baseColor: '#cccccc',
  edgeColor: '#000000',
});

/**
 * グリッドのサイズ
 * @constant {number}
 */
export const grid = 2.8 as const;
